# Landing Page Components

This directory contains all section components for the Scain landing page.

- Each component receives its content via props from `landingContent.ts`.
- Use Tailwind and shadcn/ui primitives for styling.
- For new sections, create a new component and add it to `app/landing/page.tsx`. 